# Gradle / build
.gradle/
build/
/captures
.externalNativeBuild
.cxx
*.apk
*.aab

# IDE
*.iml
.idea/
.DS_Store

# Local config (SDK path, secrets)
local.properties

# Scratch
.dl/

# Large binary assets — fetched at build time via scripts/fetch-assets.sh
# (the Kokoro model exceeds GitHub's 100 MB file limit, so it is not committed)
app/src/main/assets/kokoro-int8-en-v0_19/
app/src/main/jniLibs/
